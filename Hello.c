#include <stdio.h>
// Aria of circle

int main()
{
    float radius;
    printf("Enter radius");
    scanf("%f", &radius);
    printf("area is : %f", 3.14 * radius * radius);

    return 0;
}