#include <stdio.h>
int main()
{
    // FILE *fptr;
    // fptr = fopen("test2.txt","r");

    // char ch;
    // ch = fgetc(fptr);
    // while(ch != EOF){
    //     printf("%c",ch);
    //     ch = fgetc(fptr);
    // }

    // fclose(fptr);
    // FILE *fptr;
    // fptr = fopen("INT.txt", "r");
    // int n;

    // fscanf(fptr, "%d", &n);
    // printf("number = %d \n", n);
   

    // fclose(fptr);

    return 0;
}